# WatchDash Privacy Policy

Static GitHub Pages privacy policy for WatchDash.

Deploy this folder as the Pages root. It includes:

- `index.html` for the privacy policy
- `.nojekyll` so GitHub Pages serves files directly
- `404.html` as a simple redirect fallback
